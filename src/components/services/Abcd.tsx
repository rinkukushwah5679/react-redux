import React from 'react'
import { View, Text } from 'react-native'
import {sdcdcsd} from '../services/abc'
import {ADD_TO_CART} from '../services/constants'
const abcd = () => {
	return (
		<View>
			<Text>{sdcdcsd}, {ADD_TO_CART}</Text>
		</View>
	)
}

export default abcd